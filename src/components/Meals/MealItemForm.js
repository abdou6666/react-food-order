import classes from "MealItemForm.module.css"
const MealItemForm = () => {
    return (
        <form className={classes.form}>
            <button className={classses.button}></button>
        </form>
    )
}

export default MealItemForm
